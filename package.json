{
  "name": "anti-fraud-arcade",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node esbuild.config.mjs --watch",
    "build": "node esbuild.config.mjs",
    "check": "tsc --noEmit",
    "preview": "npx --yes serve web-build -l 5173",
    "deploy": "gh-pages -d web-build -m \"deploy: web build\""
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "gh-pages": "^6.1.0",
    "typescript": "~5.8.3"
  }
}
